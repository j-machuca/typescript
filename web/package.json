{
    "devDependencies": {
        "typescript": "^5.0.4"
    },
    "dependencies": {
        "axios": "^1.3.6"
    },
    "scripts": {
        "start:db": "json-server -w db.json",
        "start:parcel": "parcel index.html"
    }
}
